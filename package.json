{
  "name": "toplines",
  "version": "1.0.0",
  "description": "application to fetch Stockholm bus lines",
  "scripts": {
    "start": "concurrently 'npm start --prefix server' 'npm start --prefix client'",
    "test": "npm test --prefix server && npm test --prefix client"
  },
  "keywords": [],
  "author": "arjunreddyk",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
